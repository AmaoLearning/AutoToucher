<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ── 权限 ── -->
    <!-- 前台服务 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <!-- 开机自启 -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- 通知（Android 13+） -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- 精确闹钟（Android 12）-->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <!-- 精确闹钟（Android 13+，更强豁免） -->
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <!-- 锁屏唤醒：保持 CPU 唤醒状态直到 WakeActivity 接管 -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Android 14+：ACQUIRE_CAUSES_WAKEUP 的屏幕唤醒权限 -->
    <uses-permission android:name="android.permission.TURN_SCREEN_ON" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AutoToucher">

        <!-- ── 主 Activity ── -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ── Phase 2：无障碍服务（手势注入）── -->
        <service android:name=".service.AutoAccessibilityService"
            android:exported="true"
            android:label="@string/accessibility_service_label"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- ── Phase 2：前台执行服务（specialUse 类型，API 34+ 要求声明）── -->
        <service android:name=".service.TaskExecutorService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="自动执行预设点击任务" />
        </service>

        <!-- ── Phase 3：闹钟广播接收器（由 AlarmScheduler 触发）── -->
        <receiver android:name=".scheduler.AlarmReceiver"
            android:exported="false" />

        <!-- ── Phase 3：开机广播接收器（重启后恢复所有闹钟）── -->
        <receiver android:name=".scheduler.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- ── Phase 6：锁屏唤醒 Activity（任务执行前唤亮屏幕 / 解除锁屏）── -->
        <activity
            android:name=".ui.WakeActivity"
            android:exported="false"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:noHistory="true"
            android:excludeFromRecents="true"
            android:theme="@style/Theme.AutoToucher.Transparent" />

    </application>

</manifest>
